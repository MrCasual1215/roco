# 个人实训报告：Sort Cubes 与 Move Rope 任务优化

## 1. 个人负责内容

本次实训中，我主要负责 RocoBench 多机器人协同操作中的两个任务：

1. **Sort Cubes**：三台机器人 Alice、Bob、Chad 协作，将不同 cube 移动到对应目标 panel。
2. **Move Rope**：Alice 和 Bob 协作抓住绳子两端，将柔性绳子越过障碍墙并放入窄槽。

我的主要工作不是单纯修改 prompt，而是围绕大模型规划在真实物理环境中容易出现的不可执行问题，加入任务级约束、动作校验、失败恢复和确定性推荐策略，使 LLM 生成的计划更容易被 parser、IK、RRT 和环境执行模块接受。

---

## 2. Sort Cubes 任务优化

### 2.1 任务特点与初始问题

Sort 任务要求三个机器人把 cube 放到固定目标：

- `blue_square -> panel2`
- `pink_polygon -> panel4`
- `yellow_trapezoid -> panel6`

由于机器人可达范围不同，跨区域转运必须通过共享 handoff panel 完成：

- Alice 与 Bob 通过 `panel3` 交接；
- Bob 与 Chad 通过 `panel5` 交接。

实验初期主要问题包括：

1. LLM 会输出格式正确但物理上不可执行的动作，例如让 Bob 抓取实际够不到或 IK 不可解的 cube；
2. 缺少方向性 handoff 约束，cube 可能被放到后续机器人无法处理的位置；
3. 多机器人同时动作时容易出现碰撞；
4. 失败动作屏蔽过粗，一个机器人失败会误伤其他可执行动作；
5. 执行中可能出现机器人已经 holding cube，但下一轮仍推荐 PICK 的情况。

### 2.2 我的主要改进

#### 1）加入 Sort 专用合法动作枚举

我在 `code/rocobench/envs/task_sort.py` 中加入 Sort 任务专用的 planning helper：

- `get_plan_state_prompt(obs)`：整理当前 cube 所在 panel、目标 panel、完成状态和可达机器人；
- `get_legal_actions(obs)`：根据当前环境状态生成每个机器人的合法动作；
- `get_recommended_plan(obs)`：基于合法动作生成推荐计划；
- `format_legal_actions_prompt(obs)`：把合法动作和推荐计划写入 prompt。

这样可以把模型限制在当前状态下更可能执行成功的动作集合中，减少纯自然语言 prompt 带来的不稳定性。

#### 2）加入方向性 handoff 规则

我根据 Alice、Bob、Chad 的可达范围，为 cube 转运加入方向性约束。例如跨机器人移动时必须通过 `panel3` 或 `panel5`，避免模型把物体放到后续无人可达的位置，减少任务死局。

#### 3）在 plan 层加入动作校验

我在 `code/prompting/plan_prompter.py` 中加入 LLM 输出校验逻辑，主要检查：

- 是否包含 `EXECUTE`；
- 是否每个机器人 exactly one action；
- action 是否来自 legal actions；
- 同一轮是否多个机器人操作同一个 object；
- 同一轮是否多个机器人放到同一个 target；
- 是否重复执行已经失败的 forbidden action。

不合法的动作不会进入 parser/RRT，从源头减少无效执行。

#### 4）精确屏蔽失败机器人动作

早期策略是某一轮失败后，把整轮 response 中所有非 WAIT 动作都加入 forbidden。这会导致 Bob 失败时，Alice 和 Chad 本来可执行的动作也被误伤。

我改成从环境反馈中识别失败机器人，例如 `IK failed: on Bob` 或 `Out of reach: Bob`，只屏蔽 Bob 当前失败动作，保留其他机器人的可执行动作，提高恢复能力。

#### 5）加入 deterministic partial fallback

当 LLM 多次 replan 仍失败时，系统会尝试 deterministic fallback：

- 先尝试任务推荐计划；
- 如果整体计划失败，再尝试推荐计划的子集；
- 优先执行单机器人或低风险双机器人动作。

这样可以避免因为某一个机器人 IK 失败导致整轮完全停滞。

#### 6）修复 handoff 点、IK 与 holding 残留问题

我还针对 Sort 的物理执行问题做了进一步修复：

- 调整 `panel3` / `panel5` 的共享 handoff 放置点，使其更适合“放置方能放，接收方能抓”；
- 对 Sort PICK 姿态做任务特化放松，减少 cube yaw 或 handoff 后姿态变化导致的 IK 失败；
- 加入轻量 IK precheck，提前过滤明显 IK 不可行的 PICK；
- 增加 `PLACE <object> <panel>` 形式的 PLACE-only recovery，用于处理机器人已经 holding cube 的状态；
- 尝试 safe parallel，只允许 Alice 和 Chad 在 footprint 不重叠时并行，Bob 作为中间机器人保持保守串行。

### 2.3 Sort 实验效果

| 实验版本 | 结果 | 说明 |
| --- | --- | --- |
| `sort_legal_plan_test` | 4/7 成功 | 初步加入合法动作和 fallback 后，仍有碰撞和卡死 |
| `sort_verifier_test` | 10/10 成功 | 串行 verifier 版本稳定性最高，平均约 6.1 step |
| `sort_safe_parallel_test` | 7/10 成功 | safe parallel 提高成功样本效率，平均约 4.43 step，但稳定性下降 |
| `sort_safe_parallel_recovery_test` | 9/10 成功 | 加入 recovery 和 IK 改进后，稳定性恢复到 90% |

从结果看，Sort 任务中单纯追求并行会降低稳定性，而加入 verifier、fallback、recovery 和 IK 预检查后，可以在成功率和效率之间取得更好的平衡。

---

## 3. Move Rope 任务优化

### 3.1 任务特点与初始问题

Rope 任务要求 Alice 和 Bob 分别抓住长绳两端，将绳子抬高越过障碍墙，并把两端放入窄槽。该任务比 Sort 更复杂，原因是 rope 是柔性物体，单端操作很容易把绳子拖坏、掉落或拖到机器人难以再次抓取的位置。

实验初期主要问题包括：

1. LLM 经常输出不存在的动作名，例如 `LIFT`，导致 parser 无法解析；
2. 通用 plan prompt 中混入了 Sort 专用内容，污染 Rope 任务规划；
3. prompt 允许 `WAIT`，但 Rope task feedback 又把 WAIT 判为 unsupported，前后矛盾；
4. Rope PICK 目标高度太低，容易发生桌面碰撞或 IK 失败；
5. LLM 会让某个机器人单独 PICK 或单独 PUT，导致绳子状态被破坏；
6. Rope 需要动作带 PATH，路径高度和两臂协同关系都会影响执行成功。

### 3.2 我的主要改进

#### 1）修正 Rope 动作空间，禁止非法动作名

我在 `code/rocobench/envs/task_rope.py` 中明确 Rope 合法动作只有：

- `PICK <obj> PATH <path>`
- `PUT <obj> <location> PATH <path>`
- `WAIT PATH <path>`

并在 prompt 中明确禁止：

```text
LIFT, MOVE, PLACE, LOWER, RAISE, DROP
```

原因是模型看到 “lift the rope” 这类自然语言后，容易把 `lift` 误当作动作名输出。修改后，`LIFT` 解析失败明显减少。

#### 2）修复任务 prompt 污染

我修改了 `code/prompting/plan_prompter.py` 中的 plan prompt 生成逻辑，使其优先调用每个任务自己的 `central_plan_prompt()`。

这样 Rope 使用 Rope 自己的规划提示，Sort 使用 Sort 自己的规划提示，避免 Rope prompt 中出现 cube、panel、handoff 等 Sort 任务内容。

#### 3）允许 WAIT 作为合法保守动作

原 Rope feedback 中会把 `WAIT` 判为 unsupported，这与 prompt 中允许 WAIT 矛盾。我将 WAIT 从 unsupported 列表中移除，只禁止真正非法的动作，如 `PLACE`、`MOVE`、`LIFT`。

同时由于 Rope 使用 `action_and_path` 模式，我在 prompt 中要求 WAIT 也必须带 4 个路径点，通常使用当前 gripper 位置重复四次。

#### 4）提高 Rope PICK 目标高度

实验中发现 Rope 端点可能贴近桌面，如果 PICK goal 只比 rope 端点高 0.1m，容易出现：

- `Alice-table` 碰撞；
- Bob/Alice IK failed；
- rope 端点掉低后无法重新抓取。

因此我修改了 `get_target_pos()` 中 rope 端点的 PICK 高度：

```python
ret[2] = np.clip(ret[2] + 0.35, 0.42, 0.54)
```

这样可以保证 PICK 目标既不会贴桌面，也不会超过任务允许的高度范围。

#### 5）加入 Rope 阶段化推荐计划

Rope 的关键不是让 LLM 自由决定动作，而是必须遵守阶段顺序：

1. 两个机器人先分别抓住 rope 两端；
2. 只有两端都被抓住后，才能双机器人同时 PUT；
3. 如果只有一个机器人 holding，holding 的机器人必须 WAIT，另一个机器人先 PICK 另一端；
4. 禁止同一轮混合 PICK 和 PUT；
5. 禁止单机器人 PUT。

我在 `task_rope.py` 中加入了：

- `_agent_holding_rope(obs, agent_name)`：判断机器人是否正在抓绳子；
- `_wait_action(obs, agent_name)`：生成带 PATH 的 WAIT；
- `_pick_action(obs, agent_name, rope_end)`：生成安全高度的 PICK PATH；
- `_put_action(obs, agent_name, rope_end, groove_end)`：生成越过障碍墙的 PUT PATH；
- `get_recommended_plan(obs)`：根据当前 holding 状态生成确定性的阶段计划；
- `format_legal_actions_prompt(obs)`：把 Rope 当前阶段和推荐计划写进 prompt。

推荐策略可以概括为：

```text
两人都空手：Alice PICK rope_front_end，Bob PICK rope_back_end
只有 Alice holding：Alice WAIT，Bob PICK rope_back_end
只有 Bob holding：Alice PICK rope_front_end，Bob WAIT
两人都 holding：Alice/Bob 同时 PUT 到 groove
```

#### 6）在 verifier 和 feedback 层硬约束 Rope 阶段顺序

只靠 prompt 不够，因此我在 Rope 的 `verify_plan_semantics()` 和 `get_task_feedback()` 中加入硬约束：

- 禁止 `LIFT`、`PLACE`、`MOVE` 等非法动作；
- 所有动作必须包含 `PATH`；
- 禁止同一轮混合 PICK 和 PUT；
- 禁止单机器人 PUT；
- 无人 holding 时必须双 PICK；
- 单人 holding 时必须 holding robot WAIT，empty robot PICK；
- 双人 holding 时必须双 PUT。

这样可以在执行前拦截会破坏 rope 状态的计划，减少单端操作造成的不可恢复失败。

### 3.3 Rope 实验效果

Rope 任务相比 Sort 更受 IK、路径和柔性物体动力学影响，目前主要改善体现在减少解析错误和阶段错误，并已有成功轨迹：

| 实验记录 | 结果 | 说明 |
| --- | --- | --- |
| `rope_prompt_fix/run_0` | 成功 | 轨迹为双 PICK 后双 PUT，`steps1_success_True.json` |
| `rope_prompt_fix` | 1/2 成功 | 修复 prompt 和阶段约束后的早期验证 |
| `rope_plan_5runs` | 记录中可识别 4 次成功 | 多个 run 出现 `steps1_success_True.json`，其余 run 记录不完整或未生成最终结果 |

Rope 任务仍存在部分初始状态下 Bob/Alice PICK IK failed 的问题，后续可以继续放松 Rope PICK 姿态约束，或加入 Rope 专用 deterministic fallback。

---

## 4. 遇到的困难与解决方法

### 4.1 只靠 prompt 无法保证物理可执行

无论是 Sort 还是 Rope，LLM 都可能输出格式正确但物理不可执行的计划。我的解决方法是加入任务级 legal actions、verifier 和 deterministic recommended plan，在进入 parser/RRT 之前先过滤明显错误。

### 4.2 符号合法不等于 IK 可解

Sort 中某些 handoff 点和抓取姿态会导致 IK failed；Rope 中 PICK 高度太低会导致桌面碰撞和 IK failed。我的解决方式包括修正 handoff 放置点、提高 Rope PICK 高度、加入 IK precheck 和任务特化姿态处理。

### 4.3 多机器人协同需要阶段约束

Rope 中单端操作会显著破坏柔性物体状态，Sort 中错误 handoff 会造成后续死局。因此我把任务拆成阶段，并用代码强制关键阶段顺序，而不是完全依赖模型自行理解。

### 4.4 失败恢复不能过度保守

如果失败后把整轮动作都禁掉，系统会丢失很多可执行动作。我改成只 ban 失败机器人的动作，并尝试 partial fallback，提高了恢复能力。

---

## 5. 个人收获

通过 Sort 和 Rope 两个任务，我认识到多机器人具身操作中，大模型更适合做高层意图生成，但要真正落地执行，必须结合任务规则、机器人可达性、IK、路径规划、碰撞约束和反馈恢复机制。

我的主要收获包括：

1. 学会从实验日志中定位失败原因，例如 parser error、reachability failed、IK failed、collision 和 timeout；
2. 理解了 LLM plan、parser、task feedback、RRT 和 MuJoCo 环境之间的关系；
3. 认识到 prompt 优化必须和程序化 verifier 结合，才能提升稳定性；
4. 学会为不同任务设计不同约束：Sort 更偏离散 handoff，Rope 更偏阶段协同和路径约束；
5. 学会在成功率和执行效率之间做工程权衡，例如 Sort 中串行更稳，safe parallel 更快但风险更高。

---

## 6. 后续改进方向

1. 对 Sort 任务进一步实现 symbolic planner / BFS，直接搜索最短可行动作序列；
2. 对 Rope 任务继续降低 IK 难度，例如放松 rope PICK 姿态约束；
3. 为 Rope 加入更强的 deterministic fallback，在 LLM 多次失败后自动执行阶段策略；
4. 对所有失败 run 做更细粒度分类，区分 parser、IK、collision、timeout 和 semantic invalid；
5. 将 Sort/Rope 中验证有效的任务级 verifier 思路迁移到其他任务。
